import { BlogPostList } from "@/features/blog";

const Blog = () => {
    return (
        <div className="min-h-screen bg-background">
            <BlogPostList />
        </div>
    );
};

export default Blog;
